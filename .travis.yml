language: cpp
sudo: required

install:
  # install ALSA lib
  - sudo apt-get update
  - sudo apt-get install -y libasound2-dev

script:
  - mkdir build
  - cd build
  - cmake ..
  - cmake --build .
  - CTEST_OUTPUT_ON_FAILURE=1 ctest
